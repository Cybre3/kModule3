let str = 'Hello,Kingsland';
console.log(str.indexOf('USA')); // -1
// Specialcasereturnsaspecialvaluetoindicate"notfound"

function sum(num1, num2) {

    if (typeof num !== 'number' || typeof num2 !== 'number') return false;
    return num1 + num2;
}
